docker-gc is running (Configured to run every {{ .Values.config.executionIntervalInMinutes }} minutes)

{{- if eq .Values.config.strategy "ByDate" }}
docker-gc will check every {{ .Values.config.executionIntervalInMinutes }} minutes and remove images older than {{ .Values.config.daysBeforeDeletion }} days in current configuration.
{{- else if eq .Values.config.strategy "ByDiskSpace" }}
docker-gc will check every {{ .Values.config.executionIntervalInMinutes }} minutes and remove images based on disk usage of all docker images.
Current threshold size limit is: {{ .Values.config.sizeLimitInGigabyte }} GB
{{- end }}

docker-gc will stop and remove containers in {{ .Values.config.containerStateBlacklist }} states if last finished at date is greater then {{ .Values.config.waitForContainersInBlacklistStateForDays }} days.